import LoginForm from "./LoginForm";
import { LoginContainer } from "./style";

const LoginPage = () => {
  return (
    <LoginContainer>
      <LoginForm />
    </LoginContainer>
  );
};

export default LoginPage;
